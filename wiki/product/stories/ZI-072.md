# ZI-072 — FedEx `/documents/v1/etds/multiupload` endpoint failing in production

| Field | Value |
|-------|-------|
| **Ticket** | #382188 |
| **Theme** | `Carrier Platform` |
| **App** | `Shopify MCSL` |
| **Area** | carrier-migration |
| **Pain** | 7/10 |
| **Reported** | 2026-04-02 14:23 UTC |
| **SLA Target** | 2026-04-05 14:23 UTC (72h) |
| **SLA Status** | 🔴 Breached (8 days overdue) |

---

## Ticket Summary

# Ticket #382188 — FedEx REST API multiupload document upload failing in production

- **Customer**: SatisfyRunning (via FedEx IT contact Abraham Ait Gougam)
- **Duration**: 2026-04-02 -- 2026-04-07 (ongoing)

## Timeline & Key Phases

### Phase 1: API key verification & initial testing (2026-04-02)
- PluginHive shared API keys for the PH MCSL Shopify app with FedEx IT contact Abraham. (Comment #0)
- Abraham confirmed Trade Documents Upload API is enabled for the production key. (Comment #1)
- Team ran new test: Auto Upload (during label generation) still failing; Post Upload (manual) works. Request/response XML logs shared with FedEx. (Comment #2, #3)

### Phase 2: Endpoint isolation (2026-04-06 -- 2026-04-07)
- PluginHive asked Abraham to confirm authorization for the `/documents/v1/etds/multiupload` endpoint. (Comment #4)
- Abraham confirmed API is activated; noted the payload format looked unexpected and asked if it was sent as multipart/form-data with JSON body. (Comment #5)
- Veena (PluginHive tech lead) tested: same payload works in sandbox but fails in production with "SERVICE.NOT.AVAILABLE" error. Shared production request/response. (Comment #7)
- Abraham confirmed the single `/etds/upload` endpoint works (document was inserted in FedEx imaging systems), but the `/etds/multiupload` endpoint specifically is failing. (Comment #8, #9)
- Abraham acknowledged the issue is with `/multiupload` only and is reviewing. (Comment #10)

## Open Issues

1. **FedEx `/documents/v1/etds/multiupload` endpoint failing in production** -- Returns "SERVICE.NOT.AVAILABLE" error. Single upload works; multiupload does not. FedEx IT (Abraham) is investigating. This blocks auto-upload of trade documents during label generation for high-volume merchant. (Comment #9, #10)

## Resolved Issues

None -- single upload workaround (Post Upload) is functional but not viable for high-volume operations.

## Customer Context

- France-based merchant (SatisfyRunning) using PH MCSL for Shopify with FedEx. High shipment volume requiring automated document upload.
- Ticket involves direct collaboration between PluginHive technical team (Veena, Keerthi) and FedEx IT (Abraham). Cross-referenced with FedEx API keys for multiple PluginHive products.

---

## User Story

As a **merchant whose carrier just migrated to a new API and expects everything to keep working**,
I want **carrier API migrations to be seamless — my labels, rates, and settings should just work after the switch**,
so that **I don't discover broken features one by one through failed shipments after a migration I didn't ask for**.

---

## Acceptance Criteria (Simple)

- [ ] The specific issue described in ZI-072 is resolved for this customer
- [ ] No regression on existing working functionality for other merchants
- [ ] If the issue recurs, the error message is actionable (tells the user what to fix)
- [ ] The fix is validated against the customer's specific store/order/carrier configuration
- [ ] A similar issue from a different merchant would not require a new support ticket

---

## Acceptance Criteria (Given/When/Then)

### Scenario: Direct fix for reported issue
- **Given** the merchant has the exact configuration described in ticket #382188
- **When** the merchant performs the action that previously triggered the issue
- **Then** the action completes successfully without errors
- **And** the result matches what the merchant expected

### Scenario: Fix holds for similar configurations
- **Given** a different merchant has a similar but not identical setup (different carrier account, different product mix)
- **When** they perform the same action
- **Then** the fix still applies and the action succeeds
- **And** no edge case triggers the original failure

### Scenario: Failure is self-diagnosable
- **Given** a future merchant encounters a related issue that the fix doesn't cover
- **When** the error occurs
- **Then** the error message identifies the specific field, carrier, or configuration causing the problem
- **And** the merchant can resolve it without opening a support ticket
